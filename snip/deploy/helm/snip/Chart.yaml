apiVersion: v2
name: snip
description: snip, the zero-to-prod link shortener — API, worker, and optional in-cluster Postgres and Redis (chapter 10.6).
type: application
version: 0.1.0          # the chart's own version: bump when the chart changes
appVersion: "main"      # the snip version it deploys by default (the image tag)
home: https://github.com/jawwadzafar/zero-to-prod
