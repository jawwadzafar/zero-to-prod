# dockerlab

A tiny stdlib-only web server for practising Dockerfiles — see chapter 9.2.

- `Dockerfile.naive` — the first attempt: works, but big, root, and cache-unfriendly.
- `Dockerfile` — multi-stage, cache-friendly, non-root, distroless.
